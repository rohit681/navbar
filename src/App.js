/** @format */

import "./App.css";
import Routing from "./Routing";

function App() {
  return (
    <div
      className="App"
      style={{ height: "100vh", display: "flex", flexDirection: "column" }}
    >
      <Routing />
    </div>
  );
}

export default App;
